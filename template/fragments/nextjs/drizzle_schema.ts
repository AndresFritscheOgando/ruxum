import { {{table_type}}, {{serial_type}}, text, timestamp } from "drizzle-orm/{{dialect}}-core";

// Add your tables here
// export const users = {{table_type}}("users", {
//   id: {{serial_type}}("id").primaryKey(),
//   email: text("email").notNull().unique(),
//   createdAt: timestamp("created_at").defaultNow().notNull(),
// });
